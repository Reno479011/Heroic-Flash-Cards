name: Asia Flix Canonical Rebuild In Progress

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy patch stacking disabled while canonical v2 source is installed.'
